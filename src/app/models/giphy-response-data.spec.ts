import { GiphyResponseData } from './giphy-response-data';

describe('GiphyResponseData', () => {
  it('should create an instance', () => {
    expect(new GiphyResponseData()).toBeTruthy();
  });
});
